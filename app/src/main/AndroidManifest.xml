<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.minhui.networkcapture.albion">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

   <application
      android:name=".MyApplication"
      android:allowBackup="true"
      android:usesCleartextTraffic="true"
      android:icon="@drawable/logo"
      android:label="@string/app_name"
      android:supportsRtl="true"
      android:theme="@style/Theme.AppCompat.Light.NoActionBar">

      <activity android:name="com.minhui.networkcapture.VPNCaptureActivity"
          android:theme="@style/MyMaterialTheme"
          android:exported="true">
          <intent-filter>
              <action android:name="android.intent.action.MAIN" />
              <category android:name="android.intent.category.LAUNCHER" />
          </intent-filter>
      </activity>

      <activity android:name="com.minhui.networkcapture.RadarActivities.PvPActivity" android:theme="@style/ToolbarTheme"/>
      <activity android:name="com.minhui.networkcapture.RadarActivities.HarvestingActivity" android:theme="@style/ToolbarTheme"/>
      <activity android:name="com.minhui.networkcapture.RadarActivities.MobsActivity" android:theme="@style/ToolbarTheme"/>
      <activity android:name="com.minhui.networkcapture.RadarActivities.ChestsActivity" android:theme="@style/ToolbarTheme" />

        <service
            android:name="com.minhui.vpn.service.FirewallVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse"
            android:exported="true"
            tools:replace="android:exported">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <service
            android:name=".RadarDrawView"
            android:enabled="true"
            android:foregroundServiceType="specialUse"
            android:exported="false" />

    </application>
</manifest>
